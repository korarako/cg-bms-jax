defaults:
  - ala2_ambient18_300k_bms_eta10_s1_gate2k
  - _self_

# Causal damping ablation: identical to eta10_s1 except eta=0.
# This deliberately keeps the same user-selected 128x5 backbone and 50-step
# rollout so any 2k difference is attributable to damping alone.
name: ala2_ambient18_300k_bms_eta0_s1_gate2k

training:
  damping: 0.0
